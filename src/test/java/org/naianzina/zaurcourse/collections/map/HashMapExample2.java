package org.naianzina.zaurcourse.collections.map;

import java.util.HashMap;
import java.util.Map;

public class HashMapExample2 {

    Map<Integer, String> map1 = new HashMap<>(16, 0.75f);

    /* Initial capacity * load Factor = Number of elements when the table (array that
     HashMap is used) is doubled its size
    */

    /* It is recommended that the Key be immutable.
    */
}
